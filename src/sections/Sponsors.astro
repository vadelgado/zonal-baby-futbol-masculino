---
import { SPONSORS } from "@/consts/sponsors"
---

<section class="my-40">

	<div class="mt-12 grid select-none grid-cols-2 gap-8 md:grid-cols-3">
		{
			SPONSORS.map(({ id, name, url, w, h }) => (
				<a
					class="company-link group relative flex h-24 items-center justify-center overflow-hidden md:h-32"
					title={`Visita la página de ${name}`}
					href={url}
					target="_blank"
					rel="noopener noreferrer"
				>
					<img
						loading="lazy"
						src={`/${id}.webp`}
						alt={`Logo de ${name}`}
						class={`company-logo h-${h} w-${w} px-4 transition group-hover:scale-150`}
					/>
				</a>
			))
		}
	</div>
</section>
<style>
	.company-link {
		background: linear-gradient(to bottom, rgba(255, 255, 255, 0.1) 0%, transparent 100%);
		transition: all 0.5s ease;
	}

	.company-link::before {
		content: "";
		position: absolute;
		inset: 0;
		display: block;
		background: linear-gradient(to bottom, rgba(255, 255, 255, 0.2) 0%, transparent);
		opacity: 0;
		height: 50%;
		transition:
			opacity 0.2s ease-in-out,
			height 0.2s ease-in-out;
	}

	.company-link:hover::before {
		opacity: 0.8;
		height: 90%;
		box-shadow: 0 -4px 3px rgba(50, 50, 50, 0.75);
	}

	.company-link:hover .company-logo {
		transform: scale(1.1);
	}
</style>